import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Emo Giphy Clicky Game!</h1>
        <p>Click on an image to earn points, but don't click on the same image twice!</p>
    </div>
);

export default Title;

